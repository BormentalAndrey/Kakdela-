plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    // id("kotlin-kapt")        // если используешь Dagger/Hilt — раскомментируй
    // id("com.google.dagger.hilt.android") // если Hilt
}

android {
    namespace = "com.kakdela.messenger" // ← замени на свой настоящий package !!!
    compileSdk = 35

    defaultConfig {
        applicationId = "com.kakdela.messenger" // ← тоже замени на свой
        minSdk = 21
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }

        debug {
            applicationIdSuffix = ".debug"
            versionNameSuffix = "-debug"
            // ← Вот эта строчка заставляет всегда генерировать universal APK
            isUniversalApk = true
        }
    }

    // Если вдруг у тебя старая версия AGP и ругается на isUniversalApk
    // можно так:
    // splits.abi { isUniversalApk = true }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    buildFeatures {
        viewBinding = true   // если используешь
        dataBinding = true   // если используешь
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")

    // Если используешь navigation, lifecycle и т.д. — добавь свои

    testImplementation("junit:junit:4.13.2")
    androidTestImplementation("androidx.test.ext:junit:1.2.1")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.6.1")
}
